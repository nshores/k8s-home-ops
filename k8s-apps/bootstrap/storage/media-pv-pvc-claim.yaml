apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-pv-pvc-claim
  namespace: home
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 120Gi
  storageClassName: manual
